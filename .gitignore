# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Environment and secrets (commit .env.example instead)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test and coverage
coverage/
.nyc_output/

# Caches
.turbo/
.cache/
.eslintcache

# Python (only if a service opts into Python later)
__pycache__/
*.pyc
.venv/
venv/

# OS and editor
.DS_Store
Thumbs.db
.idea/
*.swp
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# Local demo media that should not be versioned (keep curated demo set tracked)
data/**/local/

# Personal photos for pitch materials (never committed, per this repo's rule
# against personal names/images in shared files)
teammates/

# Local AI-agent guidance (kept on disk for local development, never tracked
# or committed; see decision-log.md for why these were removed from history)
/CLAUDE.md
/AGENT.md
**/CLAUDE.md
**/AGENT.md
.claude/

# Claude Code: commit shared project settings, keep personal overrides local
.claude/settings.local.json
