# packages/config

Typed environment parsing and validation, shared constants, and feature flags.
Other packages and services receive config from here; they do not read the
environment themselves.

Follows the shared rules in [../CLAUDE.md](../CLAUDE.md).
