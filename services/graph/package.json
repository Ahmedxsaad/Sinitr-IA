{
  "name": "@sinistria/graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Relationship graph and anomaly flags. Flags evidence patterns, never people.",
  "exports": {
    "./core": {
      "types": "./src/core/process.ts",
      "default": "./src/core/process.ts"
    }
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  },
  "dependencies": {
    "@sinistria/config": "workspace:*",
    "@sinistria/contracts": "workspace:*",
    "@sinistria/logger": "workspace:*",
    "@sinistria/service-kit": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
